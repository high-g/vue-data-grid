module.exports = {
  plugins: {
    autoprefixer: {
      grid: true,
      'browsers': [
        'last 2 versions',
        'Android >= 4',
        'iOS >= 9',
        'ie >= 10'
      ]
    }
  }
}
